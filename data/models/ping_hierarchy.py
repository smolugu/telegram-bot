# level 1 - Compression Structure (Primary)
    # this is the foundation
    # determines delivery mechanics
        # examples:
            # continuation recompression
            # sandwich compression
            # inside compression
            # failed expansion
            # engulfing expansion
            # mid-range compression
# level 2 - Cross-Index Alignment
    # confirmation of delivery mechanics on any asset
        # are NQ and ES telling the same story?
        # if yes, high confidence delivery
        # if no, expect inducement and liquidity games
# level 3 - HTF Bias (Context Layer)
    # tie breaker when indices dont align or disagree
    # htf tell which side is more likely to win when indices dont agree
    # HTF bias activated when:
        # Mixed Overlap
            # example: ES bullish, NQ weak
        # weak continuation structure
            # partial overlap, recompression inside larger balance
        # dual sided compression
            # sandwich compression
        # reversal vs continuation ambiguity
            # exmaple: failed expansion vs recompression
    # HTF bias becomes secondary or low priority, when structures align cleanly
        # trust the structure even if it disagrees with weekly bias
        # as accepted structure is the immediate truth

# Ping Framework
    # step 1: Determine compression structure - Primary engine
    # step 2: Determine Cross-Index Agreement - Confidence multiplier
    # step 3: Use HTF Bias only if structure mixed
# Ping framework prevents HTF bias overfitting which is a huge problem in trading systems
# One Line Rule: Structure creates the setup - HTF bias resolves the ambiguity

# Ping analysis framework
    # stage 1: Structure - Pure geometry
    # stage 2: Alignment - Cross Index Alignment
    # stage 3: Context - ATR + liquidity + HTF
    # Stage 4: Delivery Expectation - Expansion / Rocket / Flush / Reversal